import 'package:flutter/material.dart';

class CloseFriendsScreen extends StatelessWidget {
  const CloseFriendsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Close Friends')), body: const Center(child: Text('TBD')));
}
