import 'package:flutter/material.dart';

class MonetizationSettingsScreen extends StatelessWidget {
  const MonetizationSettingsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Monetization')), body: const Center(child: Text('TBD')));
}
