# Gather

This repository contains product planning docs, SQL schema migrations, and now an executable Flutter MVP under `flutter_app/`.

## Quick start
See `flutter_app/README.md` for full setup and run instructions.

## Database schema
Migration used by app: `database/migrations/001_initial_schema.sql`.
